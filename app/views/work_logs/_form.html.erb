<%= form_for(@work_log) do |f| %>
  <% if @work_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_log.errors.count, "error") %> prohibited this work_log from being saved:</h2>

      <ul>
      <% @work_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content ,I18n.t("activerecord.attributes.work_log.cotent")%><br />
    <%= f.text_area :content, :size => "60x3"  %>
  </div>
  <div class="actions">
    <%= f.submit I18n.t("submit.location.#{params[:action]}")%>
  </div>
<% end %>
